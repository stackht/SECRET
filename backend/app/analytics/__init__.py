"""Analytics layer (Phase 8).

Graph-algorithmic analysis over the knowledge graph, implemented with NetworkX
(and scikit-learn where useful). Pure functions that take NetworkX graphs and
return typed results — no I/O, so they are trivially unit-testable.
"""
