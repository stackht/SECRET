# SECRET backend — Python dependencies
# Phase 1 scaffolds imports; verified/hardened in Phase 2.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3

# Database
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
neo4j>=5.20
aiosqlite>=0.20        # SQLite async driver (unit tests)
email-validator>=2.1

# Auth
PyJWT>=2.8
passlib[bcrypt]>=1.7

# Data / analytics (used from Phase 8; installed now for parity)
pandas>=2.2
networkx>=3.3
scikit-learn>=1.5

# Structured file ingestion (XLSX). Optional: parser degrades gracefully.
openpyxl>=3.1

# HTTP client / CORS / TestClient
httpx>=0.27
python-dotenv>=1.0

# Testing (used across phases)
pytest>=8.2
pytest-asyncio>=0.23
